<%# <!doctype html>
<html>
<head>
    <style>
        /*body        { padding-top:80px; word-wrap:break-word; }*/
    </style>
</head>

<body>%>

<body>
  <% if(message1.length > 0) { %>
  <div class="alert alert-danger">
    <%= message1 %>
  </div>
  <% } %>
  <% if(message2.length > 0) { %>
    <div class="alert alert-success">
    <%= message2 %>
    </div>
  <% } %>
  <% if(message3.length > 0) { %>
    <div class="alert alert-success">
    <%= message3 %>
    </div>
  <% } %>
  <% if(message4.length > 0) { %>
    <div class="alert alert-success">
    <%= message4 %>
    </div>
  <% } %>

<div class = "container">
  <h2>Jobs Created by <%=user.local.name%></h2>
<%# <div class="floatLeft">%>
 <table class="table table-hover">
   <h3> Current Job Postings</h3>
   <thead>
     <tr>
       <th>TITLE</th>
       <th>POST DATE</th>
       <%# <th>FILLED</th>%>
     </tr>
   </thead>
   <tbody>
     <% joblists.forEach(function(joblist) { if (joblist.filled == false) {%>
     <tr class= "<%=joblist._id%>">
       <td width="700px">
         <a href="/joblists/<%= joblist._id%>" > <%= joblist.title %></a><br>
         <%= joblist.description.slice(0,150) %>
       </td>
       <td width="180px"><%= joblist.postDate.toDateString() %></td>

       <td width="100px"> <input type ="submit" value = "Position Filled" data-job-id="<%=joblist._id%>" class="btn btn-primary navigation closed"> </td>
       <td width="100px"> <input type ="submit" value = "View Applicants" onclick=location.href="/<%= joblist._id%>/applicants"  class="btn btn-info navigation" See Applicants </td>
     </tr>
     <% }}) %>
   </tbody>
 </table>
 <%# </div>%>

 <%# <div class="floatRight">%>
  <table class="table table-hover">
    <h2> Previous Job Postings</h2>
    <thead>
      <tr>
        <th>TITLE</th>
        <th>POST DATE</th>
        <%# <th>FILLED</th>%>
      </tr>
    </thead>
    <tbody>
      <% joblists.forEach(function(joblist) { if (joblist.filled == true) {%>
      <tr>
        <td>
         <a href="/joblists/<%= joblist._id%>" > <%= joblist.title %>
        </td>
        <td><%= joblist.postDate.toDateString()%></td>

        <td> <a href="/<%= joblist._id%>/applicants" class="btn btn-info navigation">See Past Applicants</a></td>
      </tr>
      <% }}) %>
    </tbody>
  </table>
  <%# </div>%>
</div>
<%# </body>
</html>%>
